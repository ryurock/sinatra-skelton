# Rakefile
require 'sinatra'
require 'sinatra/activerecord'
require 'sinatra/activerecord/rake'

$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), 'apps/lib'))
APP_FILE  = './apps/lib/app.rb'
APP_CLASS = 'App::Base'

